# Optional: Set backend WebSocket & API server host URL when hosting frontend separately on Netlify/Vercel
# If hosting combined (Node serving static dist), leave empty to use relative / same origin.
VITE_BACKEND_URL=
